version: 2.1
jobs:
  build-medium:
    machine:
      image: ubuntu-2004:current
    resource_class: arm.2xlarge
    steps:
      - run: uname -a
      - run: lscpu

workflows:
  build:
    jobs:
      - build-medium
